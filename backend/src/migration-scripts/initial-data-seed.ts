/**
 * Demo seed intentionally disabled.
 *
 * The Medusa starter ships a migration script here that seeds demo data
 * (T-shirts, an EUR/USD store, etc.) on every `medusa db:migrate`. Terra Tunis
 * seeds its own catalogue instead via `src/scripts/seed.ts`
 * (run with: `npx medusa exec ./src/scripts/seed.ts`).
 */
export default async function disabledDemoSeed() {
  // no-op
}
